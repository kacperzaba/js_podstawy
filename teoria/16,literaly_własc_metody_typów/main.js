const txt = "jakis tekst";
let score = 12.54;

txt.length;
txt[2];

txt.toUpperCase();
score.toFixed();
txt.indexOf("k");

const newTxt = txt.toUpperCase();
const fixedNumber = score.toFixed();

const myName = "kacper";
myName.length;

const userName = new String("Kacper");
const userID = new Number(2);
const famale = new Boolean(true);
